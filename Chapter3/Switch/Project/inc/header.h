#ifndef HEADER_H
#define HEADER_H

// Function escape(s,t) that converts characters like newline and tab into visible 
// escape sequences like \n and \t as it copies the string t to s
void escape(char s[], char t[]);

#endif // HEADER_H
