#ifndef HEADER_H
#define HEADER_H

// Function declaration for entab: replace spaces with tabs
void entab(char *line, int tab_width);
// Function declaration for detab: replace tabs with spaces
void detab(char *line, int tab_width);

#endif // HEADER_H
